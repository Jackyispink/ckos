# 结构化预处理与检索

项目及处理产物均位于 D:\yidui\env\ckos，Python 使用 D:\yidui\miniconda\envs\ckos\python.exe。

## 已实施

- 85 个企业实体，企业名来自报告封面，文件名保留为别名。中英文括号通过规范化统一匹配；未知名称不做模糊猜测。
- 报告版本由文件内容哈希与预处理版本组成，旧版本留存；同一文件变更后新版本生效。多个匹配报告需要用户选择，不能擅自认定最新。
- 按九大模块、综合评价、问题、建议、实施步骤、预期效果保存内容块。段落、表格行、图表和图片都有独立来源定位。
- 原生表格保留全部单元格，包括相同数值和合并单元格的 XML 路径；原生图表提取系列、类别和数值缓存。
- 图片保存于 storage/assets，以哈希去重；每次引用仍保留所在报告与模块。
- 问题与建议建立同模块候选关联，basis=same_module_candidate、review_status=pending。该关联仅代表待核实关系，不是经核实的因果或一对一对应。
- 分值冲突、模块缺失、图片待识别等信息保存到报告质量记录。当前分值校验是规则检查，不覆盖所有可能冲突。
- 企业/报告过滤在内容检索之前进行。明确企业切换不继承上一家；指代追问从最近有效回答来源确定范围。筛选与提问企业不一致时提示，不跨企业混取。
- 总体诊断按模块提取问题与建议，每模块上限3500字。仍可能省略长模块细节，不声称全文覆盖。多个企业对比、全量统计当前返回范围提示，不给不完整统计。
- 问答历史继续使用现有 PostgreSQL 表；不会删除原会话或旧引用。

## 运行

```powershell
cd D:\yidui\env\ckos
# 更新传统原文索引及结构化预处理，启动服务
.\start.ps1

# 仅运行结构化批处理；按文件与程序版本断点跳过
& 'D:\yidui\miniconda\envs\ckos\python.exe' -m app.structured

# 独立运行视觉识别，默认只处理1张；可以按批次增加limit
& 'D:\yidui\miniconda\envs\ckos\python.exe' -m scripts.recognize_images --limit 10
```

视觉识别使用已验证可用的 internvl-latest，按单进程限速，需外网和账号调用权限。结果保存为 recognized_pending_review；失败图保留待处理状态可重试。目前仅验证了1张图片，并未宣称全部图片识别完毕。与网页同时调用时共享账号限流额度。

## 检查页面

http://127.0.0.1:8000/knowledge/data

知识库内分为“数据查看”和“知识问答”。数据查看支持企业搜索、待核对筛选，按业务模块展示报告评价、问题、建议和原始图表；不展示技术编号、候选关系数量或JSON。可下载原报告，并跳转问答自动选中该报告。可通过“管理名称与内容”修正企业名、报告名、模块分类和正文；这不等于完成问题与建议关系的人工确认。

## 当前边界与下一步

这次完成了结构化基础层和严格企业范围检索。尚未完成全部图片的识别与人工复核、逐条问题—建议语义配对的确认、独立指标数据集和跨企业统计。未复核OCR不自动用于回答。

原文段落定位和图片区域关系保留；没有渲染PDF页码和OCR单元格坐标，不能用推算页码替代真实页码。正文标题识别使用规则，不规范的子标题仍需抽查修正。人物照片不因出现在报告中就能支持经营或管理结论。

结构化表：kb_companies、kb_reports、kb_blocks、kb_relations、kb_jobs。旧 documents/chunks 用于文件下载与历史兼容。生产问答在已有结构化报告时优先使用结构化范围；旧BM25仅在完全没有结构化数据时兼容使用。

## 网页上传

知识库的“数据查看”和“知识问答”页均提供“批量上传”。用户必须填写企业名称与报告名称；人工企业名优先于文件名和封面识别，持久化于 document_labels，重启和增量入库后保持有效。当前支持未加密的 .docx，最大50 MB，解压内容上限250 MB。

原件保存在 data/upload_<唯一编号>.docx，自定义报告名用于列表及检索来源展示。同名文件不覆盖；同一企业多份报告需要选择具体报告。上传时仅处理新文件，成功后立即可查。损坏文件、空白文件或入库失败不会发布，原始上传名保存在数据库以便追溯。图片OCR仍为独立后续任务。

批量选择文件后逐份填写企业名、报告名、相似度阈值和保存方式。依次检查名称、文件指纹、规范化原文指纹和原文相似度，检查通过后才保存上传文件并切片。完全重复或超过阈值的近似报告不会作为新报告入库；回收站中的原文也参与比较。失败项可重试，已成功项不再次提交。

## 名称与内容管理

数据查看中选择报告，点击“管理名称与内容”：修改企业归属与报告名，搜索正文，按模块添加、编辑或移除内容。报告删除进入回收站，内容移除可在该报告管理窗口恢复。新内容和修改内容立即用于数据查看及结构化问答，原始 Word 与历史回答快照保留。图片原件不可替换为文字，可添加说明；表格和图表的修改作为文字内容展示。

document_state 保存报告删除状态；content_edits 保存内容修改和移除状态；content_audit 记录操作及修改前数据；kb_effective_blocks 提供合并后的有效内容。未变化的原文重新入库不会覆盖人工修改。若更换原文或升级解析版本产生新版本，旧版本修改不会自动迁移，需重新核对。质量提醒仍反映原文解析结果，不因文字编辑自动视为已核对。


## 上传前重复检查与版本更新

- 名称以“企业名称＋报告名称”组合检查，统一全半角、大小写、空白和末尾 .docx；不同企业允许相同报告名。普通回收站报告仍保留名称。旧版本名称不阻止当前版本使用原名。批内同名在客户端拦截，服务端在实际上传、重命名和恢复时再次检查。
- 原文从上传临时文件中提取，不调用模型，不产生知识切片。正文与表格文字保留数字和标点；表格单元格排列参与完全重复判断；图片和原生图表文件比较 SHA-256，不包含图片文字 OCR 或语义比较。
- 相似度使用连续五字符集合的加权 Jaccard。语料中越常见的模板文字权重越低，已有原报告不足5份时使用普通 Jaccard。比较的是原文，不是人工修改后的知识内容。相似度不是重复概率，不能识别所有改写、语义重复或截图重复。
- 每份文件可配置50%～99.9%的拦截阈值，试用默认90%。完全重复始终拦截，不可通过调高阈值绕过。检查失败返回已有报告和最多3处文字差异摘要。已有原文无法读取且没有有效缓存时停止上传，不静默放行。
- 更新只允许选择同一企业的有效报告。页面先展示差异，用户明确确认后再提交。新增版本成功后旧版移入回收站，原始文件、人工修改和历史回答保留；旧版人工修改不会自动复制到新版。整条版本链都参与完全重复检查，近似检查允许更新目标及其祖先；不能用更新某份报告绕过其他报告的重复检查。
- 同名旧版恢复前须在回收站“查看与修改”中改为其他报告名。更新不覆盖已有原文件。并发上传、重命名和恢复共用 PostgreSQL 事务锁，避免检查与保存之间的名称或近似内容竞态。
- original_profiles 缓存已有原文的比较特征；document_versions 记录新旧报告关系。缓存不参与问答，不修改已入库正文。`python -m scripts.calibrate_duplicates` 可刷新原文特征并输出本地 storage/duplicate_calibration.json。现有85份报告、3570对的最高相似度约12.21%，追加短修订的模拟样本均超过99.6%；这不是有标注样本上的准确率评测，90%仍为试用阈值。


## 批量上传性能

上传窗口默认同时处理3份，可选择1、2、3份。浏览器使用有上限的工作队列，逐份展示等待、检查和结果，单份失败不影响其他文件。后端同时进行最多3份原文提取；最终名称/原文查重和发布仍使用同一数据库锁，获得锁后重新读取最新资料检查，防止并发的完全重复或近似报告同时入库。这里不是所有阶段完全并行。

相似度复用原文五字符集合与语料权重缓存；早期版本随语料变化更新权重；现在改为固定权重版本并持久化特征，见下方“持久化查重索引”。切片、结构化内容、问题建议候选关系通过 executemany 批量写入。成功上传不再重载全量旧BM25索引；结构化问答直接读取数据库中的有效资料，旧BM25只在没有结构化资料时使用。

本机85份原文的单次比较优化前约2.3秒，优化后首次约1.8秒、相同语料缓存命中约0.11秒。新增文档会更新缓存，不能将缓存命中收益当成整批入库提速倍数。原文提取、网络传输、文件大小及写入仍会影响总时长。当前上传需保持页面打开，不是支持浏览器关闭后恢复的后台任务队列。


## 持久化查重索引（当前上传使用）

当前默认快速检查，也可逐文件选择严格检查。原文提取、文件指纹、规范化正文/表格/图片图表联合指纹均保留。完全重复通过索引直接决定拒绝，不再进行不必要的候选或全库评分。

- dedup_models：固定模板权重版本。首次建库用当时资料校准；空库初次使用等权重。新增原文使用既有权重，未知五字符片段使用该版本的默认权重，不触发全量重算。
- dedup_documents：每份原文保存文件指纹、联合内容指纹、五字符特征集合、总权重和算法版本。原文件变化或解析/索引版本变化时只更新对应记录。人工内容编辑不改变原文查重依据。
- dedup_buckets：确定性64维 MinHash，32组、每组2维；整篇、具有区分度的文字、各管理模块分别分桶。分桶查询走 PostgreSQL 索引，不逐份计算相似度。回收站和旧版本继续参与查重。
- 快速模式：取所有命中分桶的候选，并补充同企业报告及显式更新目标；不限定前K份。只读取候选特征与原文摘要进行固定权重 Jaccard 精确比较。LSH 是近似筛选，且与加权评分并非同一度量，存在漏检风险；降低用户阈值并不能保证候选召回率。重要资料应选严格模式。
- 严格模式：使用同一权重版本，对全库持久化特征逐份比较，无需重新提取已有报告原文或重新构建语料权重。不能保证识别语义改写或图片文字重复。
- 页面反馈“本次精确比较候选数 / 已有资料数”。完全重复的指纹命中会直接中止，相似度阈值不影响该判断。

维护指令（使用指定Python环境）：

```powershell
# 首次建立或增量补齐索引；已有且有效的记录不会重算
& 'D:\yidui\miniconda\envs\ckos\python.exe' -m scripts.build_duplicate_index

# 明确需要重新校准模板权重时才执行；原子切换权重版本并重建全库索引
& 'D:\yidui\miniconda\envs\ckos\python.exe' -m scripts.build_duplicate_index --refresh-weights
```

首次为85份原文建立索引；再次运行维护命令更新数为0。3个原文追加修订样例在快速模式精确比较46/85、57/85、16/85份，均找到原稿；样例不能替代真实标注样本的召回评测，也不能承诺所有文件都比严格模式快。新增索引与报告发布同一事务完成，最终查重与发布仍受同一个锁保护。失败入库不会发布部分索引。


## 批量名称识别与核对（当前上传入口）

选择DOCX文件后，最多3份并行传输到本机 storage/upload_staging，读取封面前35段文字和原文件名，自动建议企业名与报告名。识别阶段不访问企业库进行名称检查，不调用大模型、不切片、不计算内容相似度，也不建立查重索引。重复文件各自拥有独立暂存编号，不合并或删除；全部识别完成后，页面将相同企业或报告名分组排在后面，保留原始文件名和每个文件条目，不自动加后缀改名。可筛选同名或需核对的条目。

用户可直接修改建议名称；封面与文件名出现不同企业名时展示候选及核对提示，无法识别时留空。报告名称优先保留有意义的原文件名和年份，泛化文件名才尝试使用封面标题。自动识别是规则建议，不保证企业归属准确。

只有用户点击“已核对名称，开始查重入库”，才进行批内名称检查、已有名称检查、快速/严格原文查重和正式入库。确认时发送暂存编号和修改后的名称，不再次传输大文件。成功入库后清理暂存；重名或内容重复被拒绝时保留暂存，方便调整重试。暂存24小时后不可提交，下次识别时清理过期文件；不是定时后台删除服务。关闭或刷新页面会丢失本次核对界面，需要重新选择文件；已入库资料不受影响。
