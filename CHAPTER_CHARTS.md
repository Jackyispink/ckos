# 按章节选图与 Word 导出

1. 更新代码后，在原服务窗口按 Ctrl+C，再在项目目录运行 `.\start.ps1`。浏览器按 Ctrl+F5 刷新。
2. 打开已完成的报告，展开任意章节，查看“本章图表”。每个候选可单独选择图形或“不绘图”，也可修改标题。
3. “已选入报告”表示该图已保存并将进入 Word；“不绘图”也是有效的保存结果。章节标题旁显示已选数量。
4. 展开“核对数据与原文”，检查指标、单位、年份和限定条件。数字来自正文，提取不等于事实核实；正文有误应先编辑正文。
5. 点击“下载完整Word报告”。下载会等待选图保存完成，再按当前选择生成新文件，将图表插入所属章节。下载按钮显示实际包含的图片数量。

连续年份、同一指标和同一单位的数据可选择折线图、横向条形图、纵向柱状图、面积图、散点图或棒棒糖图。面积图突出规模变化；散点图只展示观测点、不连接趋势线；棒棒糖图突出各期数值。不能确认可比关系的单项数字使用“关键数字”卡片，不把不同口径的数据强行拼成图。没有可识别数据的章节可以不绘图。数据提取与绘制均由程序执行，不调用大模型。

正文修改使旧选图失效时，页面会提示清除失效选图并重新选择；导出不会悄悄忽略这些图片。绘制或保存失败会明确报错，可重试。

## 验证

## 生成操作

- **继续生成**：跳过已完成章节，补完剩余章节并更新摘要；已完成的报告不会重复调用模型。
- **重新生成**：确认后重新调用模型撰写十章和摘要（复用已有证据，不重新搜索）。新稿全部成功后在同一数据库事务中替换正文；未完成前保留原稿。成功后旧图表选择可能失效，需要重新核对。
- **取消重新生成**：仅在重写任务运行时出现，等待当前请求结束后停止后续调用并丢弃本轮暂存新稿，保留原稿。已发出的模型请求可能仍然计费。普通继续任务显示“取消生成”，保留已经保存的章节。
- 暂存新稿在内存中，服务重启会丢弃未发布的新稿，但原报告正文不受影响。当前启动脚本采用单服务进程，运行和取消状态由该进程管理。

## 版本历史

报告操作区的“版本历史”可查看、下载和恢复旧版，也可手动“保存当前版本”。每次重新生成成功替换正文前，自动保存旧版；恢复前也会自动保存当前版。备份和替换在同一数据库事务内完成。

快照包含报告名称、研究任务、正文、摘要、证据及选图配置，并保存当时选图的数据供历史 Word 导出使用。恢复时还原这些内容；恢复期间必须没有运行中的生成任务。历史记录按保存时间倒序展示。

历史数据保存在现有 PostgreSQL 的 `industry_versions` 表内，随原报告一同删除。该功能不能找回启用前已覆盖且没有备份的正文。首次使用建议立即保存当前版本。

使用 `D:\yidui\miniconda\envs\ckos\python.exe -m pytest -q` 运行回归测试。测试数据库使用隔离 schema，临时文件位于项目 `storage/test-tmp`。`scripts/preview_chapter_charts.py` 是隔离 UI 验证服务，仅在内存副本保存选择，不改变实际报告。
